<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:baselineAligned="false"
    android:orientation="horizontal"
    tools:context="net.tuanpham.bakingtime.activities.RecipeActivity">

    <!--
    This layout is a two-pane layout for the master/detail flow.
    -->

    <fragment
        android:id="@+id/fm_recipe"
        android:name="net.tuanpham.bakingtime.fragments.RecipeFragment"
        android:layout_width="300dp"
        android:layout_height="match_parent" />

    <!-- A small divider -->
    <View
        android:id="@+id/vw_two_pane_divider"
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:background="@android:color/darker_gray" />

    <!-- LinearLayout that arranges all three Android-Me body part images vertically -->
    <LinearLayout
        android:id="@+id/ll_recipe_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:background="@android:color/white"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">


    </LinearLayout>

</LinearLayout>